name: 02-workflow events

on:
    push
jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: Show the trigger
              run: echo " i have been triggered by a(n) ${{ github.event_name}} eve"

